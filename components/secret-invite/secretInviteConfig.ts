export type SecretInviteCampaign = {
  active: boolean;
};

// The single switch for the cinematic invitation and its destination page.
export const secretInviteConfig: SecretInviteCampaign = {
  active: true,
};
